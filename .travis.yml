language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shimridev/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shimridev/docker-react npm run test
